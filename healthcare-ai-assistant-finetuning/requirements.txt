# Core fine-tuning stack (install on a CUDA GPU, e.g. Google Colab T4)
# Unsloth pulls in compatible versions of transformers, peft, trl, bitsandbytes, accelerate.
unsloth
# TRL provides SFTTrainer and DPOTrainer
trl>=0.11.0
# PEFT for LoRA / QLoRA adapters
peft>=0.13.0
transformers>=4.45.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
datasets>=3.0.0
# Utilities
pandas>=2.0.0
tqdm>=4.66.0

# ---------------------------------------------------------------------------
# Colab quick-install (run this in the first notebook cell instead of pip -r):
#   !pip install -q unsloth
#   !pip install -q --no-deps "trl<0.12" peft accelerate bitsandbytes
# ---------------------------------------------------------------------------
